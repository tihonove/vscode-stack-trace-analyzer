{
	"name": "Stack Trace Analyzer Development",
	"image": "mcr.microsoft.com/devcontainers/javascript-node:22",
	"customizations": {
		"vscode": {
			"extensions": [
				"Orta.vscode-jest"
			]
		}
	},
	"postCreateCommand": "npm install -g @vscode/vsce prettier @anthropic-ai/claude-code && npm install && echo 'Development environment ready!'",
	"remoteUser": "node",
	"mounts": [
		"source=${localWorkspaceFolder}/.git,target=/workspaces/${localWorkspaceFolderBasename}/.git,type=bind,consistency=cached"
	]
}
