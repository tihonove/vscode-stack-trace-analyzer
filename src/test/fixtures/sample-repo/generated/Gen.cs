namespace SampleRepo.Generated;

// Git-ignored generated file: `git ls-files --exclude-standard` omits it, so
// resolving a frame that points here must fall through to the filesystem walk.
public class Gen
{
    public int Value => 1;
}
